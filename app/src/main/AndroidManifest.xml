<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.admin.basics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".apicalls.LoginVolleyGetActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".apicalls.LoginVolleyPostActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".apicalls.SuccessActivity" />
        <activity android:name=".apps.MySharedPredActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".widgets.WToastActivity" />
        <activity android:name=".widgets.WTextviewActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".widgets.WButtonActivity" />
        <activity android:name=".widgets.WButtonsActivity" />
        <activity android:name=".widgets.WImageActivity" />
        <activity android:name=".apps.LoginActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".apps.WebViewActivity" />
        <activity android:name=".dialouges.MyAlertDialougeActivity" />
        <activity android:name=".apps.TextToSpeechActivity" />
        <activity android:name=".myIntents.My1Activity" />
        <activity android:name=".myIntents.My2Activity" />
        <activity android:name=".apps.MyListViewActivity" />
        <activity android:name=".mymenus.MyOptionsMenuActivity" />
        <activity android:name=".mymenus.MyContextMenuActivity" />
        <activity android:name=".mymenus.MyPopupActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->

        </activity>
    </application>

</manifest>